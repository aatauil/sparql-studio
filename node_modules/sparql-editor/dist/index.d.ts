import { EditorView, ViewUpdate } from '@codemirror/view';

declare function formatQuery(view: EditorView): boolean;

type Props = {
    parent: HTMLElement;
    onChange?: (value: string, viewUpdate: ViewUpdate) => void;
    value?: string;
};

declare function createSparqlEditor({ parent, onChange, value }: Props): EditorView;

export { type Props, createSparqlEditor, formatQuery };
