{
  "name": "sparql-studio",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "package:extension": "mkdir -p apps/web/public && cd apps/bridge-extension && zip -r ../web/public/bridge-extension.zip manifest.json src/background.js src/content.js",
    "build": "npm run package:extension && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "react-resizable-panels": "^4.8.0"
  }
}
